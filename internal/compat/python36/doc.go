// Package python36 converts legacy Python 3.6.0-era artifacts into stable Go
// contracts. It is intentionally read-only and offline-testable so the control
// plane can ingest historical configuration formats without coupling the
// central runtime to Python-specific file layouts.
package python36
