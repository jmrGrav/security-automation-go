// Package utils contains small shared helpers that do not belong to a domain
// package. It exists to centralize retry and HTTP construction logic so every
// external dependency gets the same timeout and cancellation behavior.
package utils
